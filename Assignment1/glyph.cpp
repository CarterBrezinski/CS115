/*
Pull function
{
//Pull each integer one by one into their individual
spots of the 2D array
}
Render function:
{
//Step through each integer of the array, convert each
single integer into its corresponding ASCII value
}
Mirror function:
{
//Print normally(by rows and columns) just go from
right -> left
//Convert 6's to 7's and 7's to 6's
}
Transpose function:
{
//Print by columns into the normal rows
[0][1->9] then [1][1->9], etc
into [0->9][0->9]
// Convert 8's to 9's and 9's to 8's
}
Rotate function:
{
//Print the bottom row first, then do the top row
[0][0->9] put in [9][0->9]
then
[1->9][1->9] put in [0->8][0->9]
}


#include "glyph.h"
void transpose(int &inputArray[][COLUMNS])
{
}
void mirror(int &inputArray[][COLUMNS])
{
}
void rotate(int &inputArray[][COLUMNS])
{
}
void render(int &inputArray[][COLUMNS])
{
}

*/









